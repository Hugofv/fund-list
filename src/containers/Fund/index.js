import * as FundActions from '../../store/modules/fund/actions';

import React from 'react';
import Header from '../../components/Header';

const Fund = (props) => {
  return (
    <>
      <Header />
    </>
  );
};

export default Fund;
